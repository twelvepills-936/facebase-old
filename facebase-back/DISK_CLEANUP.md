# 🧹 Очистка диска на Timeweb

## Проблема
Бэкенд занимает **8.53 ГБ** из 15 ГБ (должен занимать ~200-500 MB).

## Причины
1. **Логи Docker** накопились (самая частая причина)
2. **Старые Docker образы** не удаляются
3. **npm/yarn кеш**
4. **Множество console.log** в коде (63 вызова

---

## ✅ Решение 1: Через Timeweb Panel (проще)

### Шаг 1: Очистите логи
1. Зайдите в **Timeweb Panel**
2. Откройте приложение **old-back**
3. Перейдите в раздел **"Логи"** или **"Мониторинг"**
4. Найдите кнопку **"Очистить логи"** или похожую
5. Очистите логи старше 7 дней

### Шаг 2: Настройте ротацию логов
1. В настройках приложения найдите **"Log rotation"** или **"Логирование"**
2. Установите:
   - **Максимальный размер логов:** 500 MB
   - **Хранить логи:** 7 дней
   - **Автоочистка:** Включить

### Шаг 3: Пересоберите приложение
1. Нажмите **"Пересобрать"** / **"Redeploy"**
2. Это удалит старые образы и пересоздаст контейнер

---

## ✅ Решение 2: Через SSH (если есть доступ)

```bash
# 1. Подключитесь к серверу
ssh your-timeweb-server

# 2. Проверьте что занимает место
docker system df

# Вывод покажет:
# TYPE            TOTAL   ACTIVE   SIZE      RECLAIMABLE
# Images          10      2        5.5GB     4.2GB (76%)
# Containers      5       2        2.1GB     1.8GB (85%)
# Local Volumes   3       1        1.2GB     800MB (66%)
# Build Cache     25      0        500MB     500MB (100%)

# 3. Очистите всё неиспользуемое
docker system prune -a --volumes --force

# Это удалит:
# - Остановленные контейнеры
# - Неиспользуемые образы
# - Неиспользуемые volumes
# - Build cache

# 4. Проверьте результат
docker system df
du -sh /var/lib/docker

# 5. Очистите логи вручную (если нужно)
truncate -s 0 /var/lib/docker/containers/*/*-json.log
```

---

## 🔧 Что улучшено в коде

### 1. Добавлен `.dockerignore`
Исключает ненужные файлы из Docker образа:
- `node_modules`
- `.git`
- `*.md`
- `dist` (пересоздаётся при сборке)

### 2. Улучшен `Dockerfile`
- `yarn cache clean` - очищает кеш после установки
- `--frozen-lockfile` - быстрее устанавливает зависимости
- `NODE_ENV=production` - оптимизация Node.js

### 3. Уменьшено логирование
Замените все `console.log` на нормальный logger (winston/pino) с ротацией.

---

## 📊 Ожидаемый результат

**До:**
- Занято: 8.53 ГБ
- Свободно: 6.5 ГБ

**После:**
- Занято: ~500 MB - 1 ГБ
- Свободно: ~14 ГБ

---

## ⚠️ Профилактика

### 1. Настройте мониторинг диска
В Timeweb настройте алерты:
- Уведомление при заполнении > 80%
- Автоочистка логов каждую неделю

### 2. Используйте правильное логирование

Установите winston:
```bash
yarn add winston
```

Замените `console.log` на:
```javascript
import winston from 'winston';

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ 
      filename: 'error.log', 
      level: 'error',
      maxsize: 10485760, // 10 MB
      maxFiles: 5,
    }),
    new winston.transports.Console({
      format: winston.format.simple(),
    }),
  ],
});

// Вместо console.log
logger.info('Server started');
logger.error('Error occurred', { error });
```

### 3. Регулярно проверяйте размер
```bash
docker system df  # Каждую неделю
```

---

## 🆘 Если место всё равно кончается

1. **Увеличьте диск на Timeweb:**
   - Timeweb Panel → Настройки → Увеличить диск
   - Стоимость: ~100₽/мес за 10 ГБ

2. **Вынесите логи в отдельное хранилище:**
   - Используйте внешний сервис логирования (Sentry, LogDNA)
   - Отправляйте логи в S3

3. **Уменьшите логирование:**
   - Логируйте только ошибки в production
   - `NODE_ENV=production` и `logger.level = 'error'`

---

## ✅ Чеклист

- [ ] Очистить логи в Timeweb Panel
- [ ] Настроить ротацию логов
- [ ] Выполнить `docker system prune`
- [ ] Пересобрать приложение
- [ ] Проверить размер после: должно быть < 1 ГБ
- [ ] Настроить мониторинг диска
- [ ] Заменить console.log на winston (опционально)

---

Вопросы? Напишите в саппорт Timeweb или проверьте их документацию! 📚

