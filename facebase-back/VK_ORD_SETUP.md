# Настройка автоматического получения ERID через ВК Ордер

## Описание

Система автоматически получает ERID (идентификатор рекламы) через API ВКонтакте Ордер при создании предложения (proposal). ERID необходим для маркировки рекламы согласно требованиям законодательства РФ.

## Шаг 1: Регистрация в ВК Ордер

1. Зарегистрируйтесь на платформе [ОРД ВКонтакте](https://ord.vk.com/)
2. Создайте учетную запись оператора рекламных данных
3. Получите API ключ для доступа к API

## Шаг 2: Настройка переменных окружения

Добавьте в файл `.env`:

```env
# VK Ord Integration
USE_VK_ORD=true
VK_ORD_API_KEY=your-vk-ord-api-key-here
VK_ORD_API_URL=https://api.vk.com/ord/v1
```

**Важно:** 
- `USE_VK_ORD=true` - включает автоматическое получение ERID
- `VK_ORD_API_KEY` - ваш API ключ от ВК Ордер
- `VK_ORD_API_URL` - URL API (по умолчанию: `https://api.vk.com/ord/v1`)

## Шаг 3: Как это работает

### Автоматическое получение ERID

При создании предложения (`POST /api/proposals`):

1. **Если `erid` не передан в запросе**, система автоматически:
   - Получает данные проекта и канала
   - Создает креатив в ВК Ордер через API
   - Получает `erid` из ответа
   - Сохраняет `erid` в предложении

2. **Если `erid` передан в запросе**, используется переданное значение

### Пример запроса

```json
POST /api/proposals
{
  "channelId": "507f1f77bcf86cd799439011",
  "initiatorId": "507f1f77bcf86cd799439012",
  "projectId": "507f1f77bcf86cd799439013"
  // erid не указан - будет получен автоматически
}
```

### Пример ответа

```json
{
  "_id": "507f1f77bcf86cd799439014",
  "channelId": "507f1f77bcf86cd799439011",
  "initiatorId": "507f1f77bcf86cd799439012",
  "projectId": "507f1f77bcf86cd799439013",
  "erid": "ERID123456789", // Автоматически получен
  "status": {
    "value": "waiting_channel_approval",
    "details": [...]
  }
}
```

## Шаг 4: Проверка работы

1. Убедитесь, что `USE_VK_ORD=true` в `.env`
2. Создайте предложение через API без указания `erid`
3. Проверьте логи сервера - должны появиться сообщения:
   - `📤 Creating creative in VK Ord...`
   - `✅ ERID получен от ВК Ордер: ERID123456789`
4. Проверьте, что предложение содержит `erid` в ответе

## Обработка ошибок

Если автоматическое получение ERID не удалось:

- Предложение все равно создается (без `erid`)
- В логах появляется предупреждение
- Можно вручную добавить `erid` через `PUT /api/proposals/:id`

## Отключение автоматического получения

Чтобы отключить автоматическое получение ERID:

```env
USE_VK_ORD=false
```

В этом случае:
- Система не будет обращаться к API ВК Ордер
- Предложения создаются без автоматического получения `erid`
- Можно вручную указать `erid` при создании предложения

## API Endpoints

- `POST /api/proposals` - создание предложения (автоматически получает erid)
- `PUT /api/proposals/:id` - обновление предложения (можно указать erid)

## Логирование

В режиме разработки все операции с ВК Ордер логируются:

- `📤 Creating creative in VK Ord...` - начало создания креатива
- `✅ ERID получен от ВК Ордер: ...` - успешное получение
- `⚠️ Не удалось автоматически получить ERID` - ошибка получения
- `❌ Ошибка при создании креатива в ВК Ордер` - детали ошибки

## Дополнительная информация

- [Документация ОРД ВКонтакте](https://ord.vk.com/docs)
- [API ВК Ордер](https://api.vk.com/ord/v1/docs)

